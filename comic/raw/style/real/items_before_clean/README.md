# real / items_before_clean —(通常用不到)

畫風 **real(真實照片/梗圖)**:照片**整張直接當配件貼、不去背**(像梗圖那樣用),所以直接把 .jpg
放進 `../items/` 並在 `js/items.js` 的 `STYLES.real.items` 登記即可,不必經過去背收件匣。

這個資料夾留給「真的想對某張真實照片做去背」的少數情況(那才丟這裡,用 `ItemCut` 處理)。
命名取主體 WordNet(chameleon、cat、child…),梗/主題放 tags。
